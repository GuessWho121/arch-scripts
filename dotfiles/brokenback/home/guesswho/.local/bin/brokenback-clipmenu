#!/usr/bin/env sh
set -eu

export CM_LAUNCHER=rofi
export CM_SELECTIONS=clipboard
export CM_DIR="${XDG_CACHE_HOME:-$HOME/.cache}/clipmenu"

if ! pgrep -u "${USER}" -x clipnotify >/dev/null 2>&1; then
  clipmenud >/dev/null 2>&1 &
  sleep 0.2
fi

clipmenu
